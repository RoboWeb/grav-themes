<!-- <partials/metadata> -->
{% for meta in page.metadata %}
    <meta {% if meta.name %} name="{{ meta.name }}" {% endif %} {% if meta.http_equiv %} http-equiv="{{ meta.http_equiv }}" {% if meta.charset %} charset="{{ meta.charset }}" {% if meta.property %} property="{{ meta.property }}" {% if meta.content %} content="{{ meta.content }}"/>
{% endfor %}
<!-- </partials/metadata> -->